# Seed data

Drop the CSVs from `career_guardian_dataset.zip` here (`personas.csv`, `current_skills.csv`, `role_tasks.csv`, `skill_gaps.csv`, `signals.csv`, `resources.csv`, `plans_30_60_90.csv`, `pathfinder.csv`, `progress.csv`) so everyone loads the same seed data into DynamoDB. See `../schema.md` for how each file maps to its table.
